# Firebase Console > Project settings > Your apps > Web app > SDK configuration.
# These are PUBLIC browser configuration values, not admin credentials.
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_APP_ID=
# No DATABASE_URL, APP_PASSWORD, SESSION_SECRET, or service-account key is needed.
