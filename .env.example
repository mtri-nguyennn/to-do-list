# Leave DATABASE_URL empty for automatic SQLite storage during local development.
# Production: Render external PostgreSQL URL, with ?sslmode=require on Vercel.
DATABASE_URL=
# Production workspace password. Use a long random value.
APP_PASSWORD=
# Separate secret of at least 32 characters. Generate with: openssl rand -hex 32
SESSION_SECRET=
